export type NeutralNetworkSettings = {
  scale: number;
  seed: number;
  grid: {
    visible: boolean;
    opacity: number;
    divisions: number;
    strokeWidth: number;
  };
  blueprint: {
    darkness: boolean;
  };
  form: {};
};
